[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "codemind"
version = "0.1.0"
description = "Local coding agent harness with post-training data utilities"
requires-python = ">=3.10"
dependencies = []

[project.optional-dependencies]
train = [
  "torch",
  "transformers",
  "peft",
]

[project.scripts]
codemind = "pico.cli:main"

[dependency-groups]
dev = [
  "pytest>=8.3.5",
  "ruff>=0.4.4",
]

[tool.setuptools]
packages = ["pico"]
